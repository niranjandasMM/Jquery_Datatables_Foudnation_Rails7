<%= form_with(model: user_virus) do |form| %>
  <% if user_virus.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_virus.errors.count, "error") %> prohibited this user_virus from being saved:</h2>

      <ul>
        <% user_virus.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_area :email %>
  </div>

  <div>
    <%= form.label :password, style: "display: block" %>
    <%= form.text_area :password %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
